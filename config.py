"""
Configuration file for the Chatbot..

TODO:
1. Import os and dotenv to load environment variables
2. Create a function or class to load configuration
3. Get the Gemini API key from environment variables (use os.getenv or dotenv)
4. Set default values for:
   - Chunk size (how many characters/tokens per chunk)
   - Chunk overlap (how much overlap between chunks)
   - Model name (e.g., 'gemini-pro')
   - Temperature (for response generation)
5. Create a function to validate that the API key exists
6. Return a configuration object or dictionary with all settings
"""

